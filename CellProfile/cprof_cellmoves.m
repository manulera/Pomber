function [ handles ] = cprof_cellmoves( handles )
% axes(handles.ax_1)
% [~,x]=ginput(2);
% ind = floor((x-1)/handles.cropsize(1))+1;
% sour = ind(1);
% dest = ind(2);
% 
% mask = draw_cell( handles.mini_video{handles.dic}(:,:,handles.list(ind)));
end

